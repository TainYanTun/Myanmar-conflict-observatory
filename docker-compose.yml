services:
  app:
    build: .
    container_name: conflict-dashboard
    restart: always
    environment:
      DB_URL: ${DB_URL}
      DB_TABLE: ${DB_TABLE:-conflict_events}
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data

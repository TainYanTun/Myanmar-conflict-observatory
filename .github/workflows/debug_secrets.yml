name: Debug Workflow

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest
    environment: Daily Data Ingestion
    steps:
      - name: Debug Environment Secrets
        run: |
          echo "ACLED_EMAIL is: ${{ secrets.ACLED_EMAIL && 'SET' || 'NOT SET' }}"
          echo "DB_URL is: ${{ secrets.DB_URL && 'SET' || 'NOT SET' }}"

